"""FastAPI-only helpers used during the Flask→FastAPI migration.

This package is intentionally small and does not introduce a new app factory yet;
it provides middleware + response helpers that let us get parity with Flask.
"""
