import { subscriptionsGetCurrent } from '../api/generated/xEarCRMAPIAutoGenerated';
import type { SubscriptionsGetCurrent200Data } from '../api/generated/xEarCRMAPIAutoGenerated.schemas';

export type SubscriptionInfo = SubscriptionsGetCurrent200Data | undefined;

export const subscriptionService = {
    getCurrentSubscription: async (): Promise<SubscriptionInfo> => {
        const response = await subscriptionsGetCurrent();
        return response.data?.data;
    }
};
